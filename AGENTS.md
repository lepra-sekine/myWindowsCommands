# AGENTS.md

## 1. プロジェクト概要

このリポジトリは、Windows 環境向けの自作コマンドやスクリプト、Chrome 拡張をまとめた小さな実験・ツール置き場です。

- Windows の `.cmd` / `.bat` / PowerShell `.ps1` を中心に管理します
- 開発用途の補助スクリプトや、ブラウザ操作の自動化を含みます
- Chrome 拡張については、DOM 構造依存やブラウザ操作の自動化が前提のため、可読性と安全性を重視します

## 2. 実装時の基本方針

1. まず最小限で、意図が分かるコードを優先する
2. 変数名・関数名・クラス名は役割が明確になるよう命名する
3. 「分かりにくい処理」には必ずコメントを書く
4. クラスや関数には、ブロックコメントで責務と型を明示する
5. 実装後は、該当環境に応じて構文や実行チェックを行う

## 3. コメント必須ルール

### 3.1 分かりにくい処理にはコメントを書く

どの言語でも、次のような処理にはコメントを必ず追加すること:

- 条件分岐が複雑な箇所
- ループと状態更新が混在している箇所
- 例外や失敗時の判定
- DOM 参照・XPath・CSS セレクタの解釈が曖昧な箇所
- Windows コマンドや PowerShell なら、標準出力や引数処理の意図が不明瞭な箇所
- 何をしているか一読では分かりにくい正規表現や文字列変換

コメントは「何をしたか」だけでなく、「なぜそうするのか」を書くこと。

### 3.2 クラス・関数にはブロックコメントを必ず付ける

すべてのクラスと関数には、以下を含むブロックコメントを付けること。

- 何のクラスなのか / 何の関数なのか
- 受け取る引数の型と意味
- 返却値の型と意味
- 例外や副作用がある場合の注意点

#### 例: JavaScript / TypeScript

```js
/**
 * 指定した投稿要素から削除ボタンを押して、投稿を削除する処理を実行する。
 *
 * @param {Element} postElement - 削除対象の投稿要素
 * @param {number} retryCount - 再試行回数
 * @returns {Promise<boolean>} 削除に成功した場合は true、失敗した場合は false
 */
async function deletePost(postElement, retryCount) {
  // 処理内容
}
```

#### 例: PowerShell

```powershell
<#
.SYNOPSIS
指定されたファイルをバックアップして、古いファイルを整理する。

.DESCRIPTION
対象ディレクトリのファイルを走査し、指定日数超過の古いバックアップを削除する。

.PARAMETER Path
対象ディレクトリのパス

.OUTPUTS
System.Boolean
処理が正常終了した場合は $true を返す。
#>
function Remove-OldBackup {
    param(
        [string]$Path
    )
}
```

### 3.3 既存コードのコメントスタイルを保つ

- 新しい実装では既存ファイルのコメント粒度に合わせる
- コメントは過剰に書くのではなく、読み手が迷う箇所にだけ集中する
- 仕様が変わったらコメントも同期して更新する

## 4. 言語別ルール

### JavaScript / Chrome 拡張

- DOM 依存のコードでは、対象要素の取得条件とタイミングをコメントで補足する
- 実行順序が不安定な箇所では、待機や再試行の理由を明記する
- `manifest.json` と実装コードの整合性を保つ

### PowerShell / CMD

- 引数の前提条件と利用方法をコメントに含める
- エラー時の挙動を明示し、失敗時に何を返すかを記述する
- 直接実行できる前提で、依存するディレクトリや環境変数を明示する

## 5. 検証の基本

変更を行う際は、以下のような最小限の確認を行うことが望ましい。

- JavaScript: `node --check <file>`
- JSON: `python -m json.tool <file>`
- PowerShell: `powershell -NoProfile -ExecutionPolicy Bypass -File <file>`

実装の確認が必要な場合は、対象に応じた最小の検証を実施する。

## 6. 追加で意識すること

- 実行前の確認が必要な危険な処理には、明示的な警告コメントを残す
- リポジトリの README を参照して、用途と注意事項を壊さない
- 既存のコマンドや拡張の命名規則と合わせて実装する

## 7. 期待する振る舞い

AI エージェントがこのリポジトリでコードを編集する際は、以下を守る。

- 分かりにくいロジックに必ずコメントを書く
- クラスと関数はブロックコメントで説明を書く
- 引数と返却値の型を明示する
- コードの意図が伝わる命名を優先する
- 既存の仕様や README の注意事項を無視しない
